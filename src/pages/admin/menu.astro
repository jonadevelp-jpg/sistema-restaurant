---
import AdminLayout from '../../layouts/AdminLayout.astro';
import CategoryManager from '../../frontend/components/admin/CategoryManager';
import MenuItemManager from '../../frontend/components/admin/MenuItemManager';
import MenuAuthGuard from '../../frontend/components/admin/MenuAuthGuard';

// Obtener tab activo de la URL
const activeTab = Astro.url.searchParams.get('tab') || 'items';
---

<AdminLayout title="Gestión del Menú - Admin">
  <MenuAuthGuard client:load activeTab={activeTab} />
</AdminLayout>


