# ============================================
# CONFIGURACIÓN DEL SERVICIO DE IMPRESIÓN LOCAL
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo a .env: copy env.example .env
# 2. Edita .env y completa los valores
# 3. NO subas .env a Git (contiene información sensible)

# ============================================
# SERVIDOR HTTP
# ============================================

# Puerto del servicio HTTP (para compatibilidad con web)
PRINT_SERVICE_PORT=3001

# Token de seguridad para peticiones HTTP
# Genera un token seguro y reemplaza este valor
PRINT_SERVICE_TOKEN=tu-token-seguro-aqui

# ============================================
# CONFIGURACIÓN DE SUPABASE (PARA POLLING)
# ============================================

# URL de tu proyecto Supabase
# Obtener en: Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave de servicio (service_role) - NECESARIA para polling
# Obtener en: Supabase Dashboard > Settings > API > service_role key
# ⚠️ IMPORTANTE: Esta clave tiene permisos completos, mantenerla segura
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key-aqui

# ============================================
# CONFIGURACIÓN DE POLLING
# ============================================

# Habilitar polling automático (true/false)
# Si es false, solo funcionará vía HTTP
POLLING_ENABLED=true

# Intervalo de polling en milisegundos
# 3000 = 3 segundos (recomendado)
# 1000 = 1 segundo (más carga en BD)
# 5000 = 5 segundos (menos carga)
POLLING_INTERVAL_MS=3000

# ============================================
# CONFIGURACIÓN DE IMPRESORAS
# ============================================

# Impresora de Cocina (para comandas)
PRINTER_KITCHEN_TYPE=usb
PRINTER_KITCHEN_PATH=vport-usb:

# Formatos de puerto comunes en Windows:
# - vport-usb: (puerto virtual USB)
# - USB002, USB003 (puerto USB virtual)
# - COM3, COM4 (puerto COM)
# - LPT1 (puerto paralelo, no recomendado)

# Si tu impresora es de red, usa esto en lugar de lo anterior:
# PRINTER_KITCHEN_TYPE=network
# PRINTER_KITCHEN_IP=192.168.1.100
# PRINTER_KITCHEN_PORT=9100

# Impresora de Caja (para boletas)
PRINTER_CASHIER_TYPE=usb
PRINTER_CASHIER_PATH=vport-usb:

# Si tu impresora es de red, usa esto en lugar de lo anterior:
# PRINTER_CASHIER_TYPE=network
# PRINTER_CASHIER_IP=192.168.1.101
# PRINTER_CASHIER_PORT=9100

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 
# 1. Para encontrar el puerto USB correcto:
#    - Panel de Control > Dispositivos e impresoras
#    - Clic derecho en tu impresora > Propiedades de impresora
#    - Pestaña "Puertos" > Busca el puerto marcado
#    - Formatos comunes: vport-usb:, USB002, USB003, COM3, COM4
#
# 2. El polling consulta la BD cada POLLING_INTERVAL_MS milisegundos
#
# 3. Si POLLING_ENABLED=false, el servicio solo responderá a peticiones HTTP
#
# 4. El SUPABASE_SERVICE_ROLE_KEY es diferente a la ANON_KEY
#    - Service role key: Tiene permisos completos (para polling)
#    - Anon key: Permisos limitados (para frontend)
#
# 5. NO subas el archivo .env a Git (contiene información sensible)

